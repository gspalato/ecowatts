import { SafeAreaView, View } from 'react-native';

import SafeView from '@/components/SafeView';
import { TabPageContainer } from '@/components/TabPageContainer';

const Page = () => {
	return <TabPageContainer style={{ flex: 1 }}></TabPageContainer>;
};

export default Page;
